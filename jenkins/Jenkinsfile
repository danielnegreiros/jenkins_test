pipeline {
    agent any

    stages {
        stage("Build") {

           steps {
                dir("jenkins_test")
                ansiblePlaybook disableHostKeyChecking: true, installation: 'Ansible', playbook: 'ansible/playbook.yml'
            }
        }
    }
}
