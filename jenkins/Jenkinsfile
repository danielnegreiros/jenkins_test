pipeline {
    agent any

    stages {
        stage("Build") {

           steps {
                ansiblePlaybook disableHostKeyChecking: true, playbook: './ansible/playbook.yml'
            }
        }
    }
}
