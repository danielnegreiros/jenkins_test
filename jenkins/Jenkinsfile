pipeline {
    agent {
        docker { image 'ansible' }
    }

    stages {
        stage("Build") {

           steps {
                dir ("./ansible"){
                    sh 'ansible-playbook ./playbook.yml'
                }
            }
        }
    }
}
