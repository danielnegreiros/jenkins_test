pipeline {
    agent any

    stages {
        stage("Build") {

           steps {
                sh 'tree'
                // ansiblePlaybook disableHostKeyChecking: true, playbook: './jenkins_test/ansible/playbook.yml'

            }
        }
    }
}
